--- Stays because LuaRules/Configs is where the spawner looks; moving the roster is not the
--- same change as moving the spawner.

return VFS.Include("modules/scavengers/lib/defs_build.lua").FromEngine()
